version: '3'

services:

    jenkins:
        ports:
            - 8081:8080

    redmine:
        ports:
            - 8082:80

    postgres:
        environment:
            DB_USER: redmine
            DB_PASS: password
